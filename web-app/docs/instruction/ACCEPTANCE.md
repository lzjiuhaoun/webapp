# ACCEPTANCE.md — instruction 指令模块

## 模块验收标准

| 场景 | 步骤 | 预期结果 |
|------|------|---------|
| 指令下发 | 封装并发送指令 | 指令到达目标设备 |
| ACK确认 | 设备返回ACK | 指令状态更新为已确认 |
| 超时重试 | ACK超时 | 自动重试，达到上限标记失败 |
| 指令记录 | 查询历史记录 | 指令记录完整可追溯 |
